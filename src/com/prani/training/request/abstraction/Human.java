package com.prani.training.request.abstraction;

public abstract class Human {
    //abstract classes are defined with a keyword abstract, they behave same like interfaces
    //this class can only have abstract methods.
    //abstract methods: Abstract methods don't have body, their definition will be given in the child classes extending
    // abstract class
    abstract boolean hasMoustache();
}
